# TODO

**strx**

- Nodes in a tree should be just another tree, which can be rendered independently. Styles may be applied to the render function. Same for tables?
- Add fuzzy match.

**fmtx**

- Adding a Repr(any).

**fsx**

- Add zip, gzip, bzip2 and tar functions
- Add OpenFile

**syncx**

- Add goroutine pool

**convx**

- Str convs and parsers (ToString(), ToInt, ToFloat, ...)

**iterx**

- Enumerate